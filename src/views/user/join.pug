extends ../base

block content
    if errorMessage 
        span=errorMessage
    form(method="POST")
        input(placeholder="e-mail", type="text", name="email", required)
        input(placeholder="Password", type="text", name="password", required)
        input(placeholder="Confirm password", type="text", name="password2", required)
        input(placeholder="Username", type="text", name="username", required)
        input(value="Join Webster!", type="submit")