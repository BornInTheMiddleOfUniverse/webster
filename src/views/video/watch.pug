extends ../base

block content     
    video(src="/" + video.videoPath, controls)
    div
        p=video.description
        small=video.createdAt
        br
        small Uploaded by
        p=video.description
        p=video.hashtags
        //if user
        a(href=`${video.id}/edit`) Edit Video &rarr; 
    